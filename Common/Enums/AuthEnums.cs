namespace TheSeatLineApi.Common.Enums
{
    public enum UserRole
    {
        Admin = 1,
        User = 2
    }
    public enum UserStatus
    {
        Active = 1,
        Inactive = 2,
        Blocked = 3
    }
}
